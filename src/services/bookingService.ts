import type { BookingReservation } from '../types/booking'

export const bookingService = {
  async listBookings(): Promise<BookingReservation[]> {
    return []
  },
}

// TODO: Future booking/reservation CRUD.
